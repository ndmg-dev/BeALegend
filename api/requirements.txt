# Dependências de runtime da API na Vercel. Subconjunto do apps/api/pyproject.toml:
# sem uvicorn (a Vercel provê o servidor ASGI), sem alembic (migrations rodam do
# seu PC contra o Neon), sem apscheduler (o cron externo chama /api/internal/tick),
# sem openpyxl (o seed roda local).
fastapi>=0.115
sqlalchemy[asyncio]>=2.0.31
asyncpg>=0.29
pydantic[email]>=2.8
pydantic-settings>=2.4
python-jose[cryptography]>=3.3
passlib[argon2]>=1.7.4
argon2-cffi>=23.1
python-multipart>=0.0.9
pywebpush>=2.0
slowapi>=0.1.9
httpx>=0.27
